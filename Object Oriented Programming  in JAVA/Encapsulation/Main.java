public class Main {
    public static void main(String[] args) {
        Box b1 = new Box();
        System.out.println(b1.getter());
        /*
         * My Observations
         ! .getter() is abstraction where we have been displayed only the necessary things i.e., .getter() method
         ! the Box class is the process of Encapsulation where the actual implementation is hidden. 
         */
    }
}
